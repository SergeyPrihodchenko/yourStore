import { Grid } from "@mui/material";

const OptionComponent = ({options}: any) => {
    console.log(options);
    
    return (
        <Grid container>
            <Grid item>

            </Grid>
        </Grid>
    );
}

export default OptionComponent;